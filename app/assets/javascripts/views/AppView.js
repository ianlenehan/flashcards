var app = app || {};

app.AppView = Backbone.View.extend({
  el: "#main",

  render: function() {
    // var appViewTemplate = $('#appViewTemplate').html();
    // TODO: Setup appViewTemlplate

  }
});
